{% extends 'base.html.twig' %}

{% block title %}{{ image.Titulo }}{% endblock %}

{% block body %}
    <h1>{{ image.Titulo }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Imagen</th>
                <td> <img src="{{ image.imagen }}" alt="{{ image.Titulo }}" width="300" height="200"></td>
            </tr>
            <tr>
                <th>Titulo</th>
                <td>{{ image.Titulo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_images_index') }}"><button class="btn btn-primary" role="button" data-bs-toggle="button">Volver al álbum</button></a>

    <a href="{{ path('app_images_edit', {'id': image.id}) }}"><button class="btn btn-primary" role="button" data-bs-toggle="button">Editar</button></a>
    <br> <br> 
    {{ include('images/_delete_form.html.twig') }}
{% endblock %}
